简单工厂模式：
    总结：根据接收的参数不同，返回同一接口的不同实现。

创建：
第一步创建一个统一的接口，描述要生成什么。对应本示例中的接口：Mouse。
第二步创建多个接口的实现类，描述我要生成的东西具体是什么。对应本示例中的两个Mouse接口的实现类：DellMouse和HPMouse。
第三步创建一个工厂类，用于针对不同的传入，统一化的生成不同的对象。对应本示例中的MouFactory类。

使用：
在main方法中实例化一个工厂类，调用工厂类中的方法的时侯对应的传入不同的参数，最终由不同的实现类（DellMouse或者HPMouse）返回不同的接口实现。